[tox]
envlist =
    py35

[testenv]
deps =
    pytest>=3.0.0
    pytest-cov
    pytest-pep8
    pytest-xdist
    pytest-flakes
    pytest-mccabe
    pytest-timeout
    -rrequirements.txt
commands =
    pytest --basetemp={envtmpdir} --cache-clear \
           -n 8 -sv --pep8 --flakes --mccabe --cov
